{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/TypeOfEnum",
  "title": "TypeOfEnum",
  "enum": [
    "read",
    "write",
    "filter",
    "search",
    "search_or_write"
  ],
  "type": "string",
  "description": "* `read` - read\n* `write` - write\n* `filter` - filter\n* `search` - search\n* `search_or_write` - search_or_write"
}